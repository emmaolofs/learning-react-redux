/*Test is declare with an it-function. 
The first parameter is a description of oour test. 
The second parameter is a function that contains the body of our test.*/

it("should pass", () => {
  expect(true).toEqual(true);
});
